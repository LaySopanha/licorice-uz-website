# Feruz Website

This is a Vite + React website for the Feruz family business.

## Getting Started

1. **Install dependencies**:
   ```bash
   npm install
   ```
2. **Run development server**:
   ```bash
   npm run dev
   ```
3. **Build for production**:
   ```bash
   npm run build
   ```

## Project Structure

- `src/components`: React components (Navbar, Hero, Gallery, Footer).
- `src/index.css`: Global styles including variables for colors (`var(--color-primary)`) and fonts.
- `public/images`: Image assets.
- `src/App.jsx`: Main application layout.

## Design

- **Font**: Poppins
- **Primary Color**: #515E3B
# licorice-uz-website
